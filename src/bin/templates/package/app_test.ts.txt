import assert from 'node:assert'
import { describe, it } from 'node:test'

import { helloWorld } from '#src/common/util.js'

await describe('Testing util module', async () => {
    await it('helloWorld', () => {
        assert.strictEqual(helloWorld(), 'Hello, World!')
    })
})
